#include <bits/stdc++.h>

int main(){

}

int** parse(std::string filename){
    std::ifstream file (filename);
    if(file.is_open()){
        while(file.good()){
            std::string content;
            file >> content;
            content
        }
        file.close();
    }
}

int part1(char* text){

}